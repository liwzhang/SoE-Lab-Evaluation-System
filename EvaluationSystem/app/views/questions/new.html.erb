<%# A veiw that shows the option to create a new question %>
<% if(current_user && current_user.admin)%>

<h1>New Question</h1>

<%= render 'form', question: @question %>

<%= link_to 'Back', questions_path %>

<% else %>

    <%= render file: 'public/403-1.html' %>

<% end %>
